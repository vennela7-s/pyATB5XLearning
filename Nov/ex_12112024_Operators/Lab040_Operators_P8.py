#he divmod() method takes two parameters x and y,
# where x is treated as the numerator and y is treated as the denominator.
# The method calculates both x // y and x % y and returns both the values.
q, r = divmod(5,2)
print(q)
print(r)
print(q,r)